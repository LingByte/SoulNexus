// Deprecated: replaced by Arco's global Notification (see utils/notification.ts).
// Kept as a no-op shim so legacy `import NotificationContainer from ...` keeps working.
const NotificationContainer = () => null
export default NotificationContainer
