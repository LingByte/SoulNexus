package constants

// Persist signal names for optional async DB listeners.
// Telephony persist signals were removed with the telephony stack.
const (
	SigSignalingLogInsert = "persist.signaling_log.insert"
)
