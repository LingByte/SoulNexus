/** Default landing route after tenant login (console home). */
export const TENANT_HOME_PATH = '/assistant-manager'

/** Default landing route after platform admin login. */
export const PLATFORM_HOME_PATH = '/tenant-management'
